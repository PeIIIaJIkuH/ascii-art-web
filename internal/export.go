package internal

import (
	// "fmt"
	"net/http"
	// "os"
)

func WriteToFile(asciiArt string, outputFileName string, w http.ResponseWriter) {
	// file, err := os.Create(outputFileName)
	// if err != nil {
	// 	http.Error(w, "Go back to the main page", 500)
	// 	return
	// }
	// content, err := file.WriteString(asciiArt)
	// fmt.Println(content)
	// if err != nil {
	// 	http.Error(w, "Go back to the main page", 500)
	// 	file.Close()
	// 	return
	// }
	// err = file.Close()
	// if err != nil {
	// 	http.Error(w, "Go back to the main page", 500)
	// 	return
	// }
}
